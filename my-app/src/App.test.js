import { render, screen } from '@testing-library/react';
import App from './App';
import {test, expect } from '@jest/globals'

/* TESTS TO CARRY OUT:
- Title is visible
- button is visible
- Input field is visible
- Button text is visible
- Input field placeholder tetx is visible
- When user types in input field, the text is visible
- When the button is clicked the user input appears in the list
*/



test('Are the elements all visible', () => {
  
});
